name: Deploy Docs

on:
  push:
    branches: ["main"]
  workflow_dispatch: # Allow manual triggering if needed

# Disabled - docs are now deployed with web app to /documentation subdirectory
jobs:
  build:
    if: false # Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Docs deployment disabled - now deployed with web app"
